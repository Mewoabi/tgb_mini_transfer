/**
 * Authentification : inscription et connexion des utilisateurs, et émission des tokens JWT.
 *
 * <p>Ce paquet porte les cas d'usage d'authentification (endpoints {@code /api/auth/**}). Il
 * s'appuie sur le domaine {@code user} (entité et dépôt) et sur le paquet {@code security}
 * (mécanique JWT, encodage des mots de passe).</p>
 */
package com.tgbsolutions.minitransfer.auth;
